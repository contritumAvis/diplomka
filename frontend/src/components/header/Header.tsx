'use client';
import TopHeader from './TopHeader';
import MiddleHeader from './MiddleHeader';

export default function Header() {
  return (
    <header>
      <TopHeader />
      <MiddleHeader />
    </header>
  );
}
